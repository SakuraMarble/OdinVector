# 从空库开始，随数据更新逐渐建立向量索引

## 1. 实验目标

本次实验用于验证 `ODinVectorDB` 原型系统是否支持如下过程：

1. 从空库开始建立索引；
2. 先使用 `BIGANN` 前 `1e8` 个向量构建初始索引；
3. 再连续 3 轮、每轮插入 `1e8` 个向量；
4. 最终得到一个可正常加载和搜索的 `4e8` 向量索引。


## 2. 实验配置

- 数据集：`BIGANN`
- 初始索引规模：`1e8`
- 最终索引规模：`4e8`
- 扩容方式：3 轮增量合并，每轮 `+1e8`
- 索引参数：`R32_L32_B6`
- 搜索参数：`#threads=12`，`beamwidth=4`
- Groundtruth 生成方式：使用官方 `utils/compute_groundtruth` 对 `BIGANN 400M` 全量数据暴力搜索生成 `top-1000` 真值

这里的 `Recall@10` 评测基于 `400M` 全量暴力真值，而不是采样近似结果，因此能够直接反映最终 `4e8` 索引的真实搜索质量。

## 3. 实验流程

整个实验分为 5 个阶段：

1. 使用 `build_disk_index` 在 `BIGANN` 前 `1e8` 个向量上构建初始索引；
2. 第 1 轮插入 `1e8` 个向量，索引规模从 `1e8` 扩展到 `2e8`；
3. 第 2 轮插入 `1e8` 个向量，索引规模从 `2e8` 扩展到 `3e8`；
4. 第 3 轮插入 `1e8` 个向量，索引规模从 `3e8` 扩展到 `4e8`；
5. 对最终 `4e8` 索引执行搜索测试，并记录搜索阶段内存消耗与 `QPS-Recall` 表现。

## 4. 各阶段资源消耗汇总

下表中的“阶段耗时”和“峰值内存”主要依据监控图坐标轴进行读取，属于近似值，用于说明量级和趋势；搜索阶段另外补充了 `qps-recall.log` 中的精确 RSS 日志。

| 阶段 | 规模变化 | 监控图文件 | 阶段耗时 | 峰值内存 | 说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 初始建库 | `0 -> 1e8` | `build_1y.png` | 约 `6300s`，约 `1.75h` | 约 `40 GB` | 前期内存较低，末段出现明显拉升 |
| 第 1 轮插入 | `1e8 -> 2e8` | `0add1.png` | 约 `26500s`，约 `7.36h` | 约 `23 GB` | 主体阶段稳定在 `17-20 GB`，末段抬升 |
| 第 2 轮插入 | `2e8 -> 3e8` | `merge2.png` | 约 `29500s`，约 `8.19h` | 约 `36 GB` | 中后段从 `26-27 GB` 升至 `31-36 GB` |
| 第 3 轮插入 | `3e8 -> 4e8` | `2merge3.png` | 约 `31000s`，约 `8.61h` | 约 `45 GB` | 大部分时间约 `37-39 GB`，末段升至约 `45 GB` |
| 搜索阶段 | `4e8` | `search_mem.png` | 约 `155s` | 图上约 `39 GB` | 日志中可读到 RSS 峰值约 `36.25 GB` |

从整体趋势上看，随着索引规模从 `1e8` 持续扩展到 `4e8`，插入阶段内存占用逐步上升，但每一轮都能在可控内存范围内完成，最终索引也能被正常加载和搜索。

## 5. 分阶段分析

### 5.1 初始 1 亿索引构建

该阶段对应 `build_disk_index` 直接在 `BIGANN` 前 `1e8` 向量上构建初始索引。

从 `build_1y.png` 可以观察到：

- 时间约 `1.75h`
- 峰值内存约为 `40 GB`

这说明初始小规模索引可以从空库直接建立成功，且构建资源消耗总体可控。

![构建 1 亿索引时的内存消耗](./build_1y.png)

### 5.2 第 1 轮插入：从 1 亿扩展到 2 亿

该阶段对应在已有 `1e8` 索引基础上，再合并 `1e8` 向量，形成 `2e8` 索引。

从 `0add1.png` 可以观察到：

- 总耗时约 `26500s`，即约 `7.36h`
- 峰值内存约为 `23 GB`

相比初始建库阶段，这一轮插入耗时明显更长，但说明系统已经能够在已有索引上继续扩容，而不是只能重建全量索引。

![从 1 亿扩展到 2 亿时的内存消耗](./0add1.png)

### 5.3 第 2 轮插入：从 2 亿扩展到 3 亿

该阶段对应继续插入第 3 个 `1e8` 向量块，将索引规模扩展到 `3e8`。

从 `merge2.png` 可以观察到：

- 总耗时约 `8.19h`
- 峰值内存约为 `36 GB`

这一阶段显示出随着索引规模增大，更新阶段的资源消耗也随之增加，但索引仍能稳定完成第二轮大规模扩容。

![从 2 亿扩展到 3 亿时的内存消耗](./merge2.png)

### 5.4 第 3 轮插入：从 3 亿扩展到 4 亿

该阶段是最后一次批量扩容，将索引规模从 `3e8` 扩展到最终的 `4e8`。

从 `2merge3.png` 可以观察到：

- 总耗时约 `8.61h`
- 末段峰值约为 `45 GB`

这说明在达到 `4e8` 规模时，系统仍然支持继续插入并完成最终索引构建。

![从 3 亿扩展到 4 亿时的内存消耗](./2merge3.png)

### 5.5 4 亿索引搜索阶段

该阶段对最终 `4e8` 索引执行搜索测试。

从 `search_mem.png` 可以观察到：

- 搜索测试总时长约 `155s`
- 峰值接近 `39 GB`


![4 亿索引搜索阶段内存消耗](./search_mem.png)

## 6. 4 亿索引的搜索效果

### 6.1 QPS-Recall 曲线

根据 `qps-recall.log` 生成的本次实验专用 `QPS-Recall` 图如下：

![BIGANN 400M QPS-Recall 曲线](./qps-recall.png)

从曲线可以直观看到：

- `L=20` 时取得最高吞吐，`QPS=3834.74`
- `L=400` 时取得最高精度，`Recall@10=96.76%`
- 随着 `L` 提升，召回率持续增加，而 QPS 逐步下降，表现出比较典型的 ANN 检索权衡关系

### 6.2 搜索结果表

`qps-recall.log` 记录的完整结果如下：

| L | I/O Width | QPS | AvgLat(us) | P99 Lat(us) | Mean Hops | Mean IOs | Recall@10 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 10 | 4 | 3528.41 | 3369.52 | 11587.00 | 14.40 | 50.10 | 54.86 |
| 20 | 4 | 3834.74 | 3104.52 | 6222.00 | 16.83 | 60.22 | 67.84 |
| 40 | 4 | 2839.01 | 4197.73 | 12991.00 | 21.37 | 78.72 | 78.00 |
| 80 | 4 | 1879.78 | 6350.59 | 13394.00 | 30.72 | 116.58 | 86.17 |
| 100 | 4 | 1452.61 | 8213.05 | 25248.00 | 35.52 | 135.89 | 88.39 |
| 200 | 4 | 1099.23 | 10877.30 | 29392.00 | 59.88 | 233.76 | 93.61 |
| 400 | 4 | 575.16 | 20816.67 | 68616.00 | 109.35 | 431.97 | 96.76 |

从结果表可以得到以下结论：

- 最终 `4e8` 索引已经能够正常搜索，而不是仅完成结构上的合并
- 在较低 `L` 下，系统已经具备较高吞吐
- 在更高 `L` 下，系统可以把 `Recall@10` 提升到 `96.76%`
- 说明渐进式插入形成的最终索引既能用，也有较好的精度-吞吐权衡能力

## 7. 结论

本次实验从“初始建库 -> 连续三轮插入 -> 最终 4 亿搜索验证”形成了完整证据链，可以支撑以下结论：

1. `ODinVectorDB` 原型系统支持从空库开始建立初始索引；
2. 在已有索引基础上，系统支持继续插入新的向量批次，而不需要重新从零构建；
3. 索引规模能够按 `1e8 -> 2e8 -> 3e8 -> 4e8` 持续扩展；
4. 各阶段内存消耗虽然随规模增长而上升，但整体仍处于可控范围；
5. 最终得到的 `4e8` 索引能够正常加载并执行搜索；
6. 相对于 `utils/compute_groundtruth` 暴力生成的 `400M` groundtruth，最终索引最高 `Recall@10` 达到 `96.76%`。

因此，本次实验可以有效证明：`ODinVectorDB` 原型系统支持从空库出发，随着向量持续更新，渐进式建立出完整的大规模向量索引。

## 8. 附件

- 搜索日志：[qps-recall.log](./qps-recall.log)
- QPS-Recall 图（PNG）：[qps-recall.png](./qps-recall.png)
- QPS-Recall 图（SVG）：[qps-recall.svg](./qps-recall.svg)
- 初始建库内存图：[build_1y.png](./build_1y.png)
- 第 1 轮插入内存图：[0add1.png](./0add1.png)
- 第 2 轮插入内存图：[merge2.png](./merge2.png)
- 第 3 轮插入内存图：[2merge3.png](./2merge3.png)
- 搜索阶段内存图：[search_mem.png](./search_mem.png)
